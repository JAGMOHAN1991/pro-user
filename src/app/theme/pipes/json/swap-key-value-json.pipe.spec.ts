import { SwapKeyValueJsonPipe } from './swap-key-value-json.pipe';

describe('SwapKeyValueJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new SwapKeyValueJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
